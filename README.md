# Projects

一个基于 Python 的多代理 / 多技能项目，使用现代依赖管理工具 **uv** 和 `pyproject.toml` 构建。  
本仓库包含 `.opencode` 目录、技能模块、notebooks、示例代码等内容，适合扩展、协作和长期维护。

---

## 🚀 环境要求

- Python 3.12+
- uv（推荐）  
  安装方式：
  ```bash
  pip install uv
